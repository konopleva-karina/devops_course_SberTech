version: '3.8'

services:
  web:
    build:
      context: ~/services
      dockerfile: flask/Dockerfile
    # command: "flask --app /usr/src/app/services/flask/hello run"
    ports:
      - "5000:5000"
    volumes:
      - ~/services/flask/:/usr/src/app/
